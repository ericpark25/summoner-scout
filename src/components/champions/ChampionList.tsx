type ChampionListProps = {
  className?: string;
};

const ChampionList = ({ className }: ChampionListProps) => {
  return <div className={`${className}`}>ChampionList</div>;
};
export default ChampionList;
